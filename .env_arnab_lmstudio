# ==============================
# 📦 DATABASE CONFIG
# ==============================

# Full connection string (many services use this directly)
DATABASE_URL=postgresql://postgres:postgres@db:5432/postgres

# Optional: Split credentials (some libraries like psycopg use individual values)
DB_HOST=db
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=postgres

# ==============================
# 🔑 AUTH / API KEYS
# ==============================

# LLM Provider APIs (used by LLM service or orchestrator)
OPENAI_API_KEY=sk-xxx
OLLAMA_BASE_URL=http://localhost:11434

# Supabase REST (if PostgREST is used)
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ==============================
# 🤖 Embedding / LLM Config
# ==============================

# Set to 'local' or 'openai' or similar
EMBEDDING_MODE=local
EMBEDDING_MODEL=bge-small-en-v1.5

# LLM Settings
LLM_PROVIDER=ollama              # or "ollama" or "openai"
OPENAI_API_KEY=sk-xxx            # required if using OpenAI
OPENAI_MODEL=gpt-4
#OLLAMA_BASE_URL=http://ollama:11434
#OLLAMA_MODEL=qwen3:1.7b
OLLAMA_BASE_URL=http://host.docker.internal:11434
OLLAMA_MODEL=Qwen3:1.7b

LLM_PROVIDER=ollama # lmstudio # or: ollama, openai
OLLAMA_BASE_URL=http://host.docker.internal:11434
OLLAMA_MODEL=Qwen3:1.7b

OPENAI_MODEL=Qwen3:1.7b-chat
OPENAI_API_BASE=http://host.docker.internal:1234/v1
OPENAI_API_KEY=not-needed